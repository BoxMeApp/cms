name: cms
description: A starting point for Dart libraries or applications.
version: 1.0.0
repository: https://github.com/BoxMeApp/cms

environment:
  sdk: ^3.9.0

# Add regular dependencies here.
dependencies:
  bloc: ^9.0.0
  # path: ^1.9.0

dev_dependencies:
  bloc_test: ^10.0.0
  build_runner: ^2.7.0
  freezed: ^3.2.0
  freezed_annotation: ^3.1.0
  lints: ^6.0.0
  test: ^1.25.6
